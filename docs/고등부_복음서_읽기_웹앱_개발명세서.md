# 고등부 복음서 읽기 인증·반별 챌린지 웹앱 개발명세서

## 0. 문서 목적

본 문서는 고등부에서 진행하는 「우리 반 복음서 완독 프로젝트」를 실제 웹앱으로 운영하기 위한 개발명세서이다.

핵심 목적은 단순한 성경 읽기 기록 앱을 만드는 것이 아니라,

> **학생이 말씀을 읽고 인증하면 → 반 공동체가 서로 격려하고 → 교사는 반별 진행상황을 확인하고 → 목표일까지 함께 완주하도록 돕는 것**

이다.

이 문서를 Cursor의 개발 지시서로 사용하여 MVP를 먼저 구현한다.

> **문서 상태 (2026-08):** MVP 핵심 기능이 구현·운영 가능한 상태이다.  
> 아래 **「0.1 현재 구현 현황」** 및 갱신된 화면·데이터 모델·네비 절을 **as-built(실제 구현)** 기준으로 유지한다.

---

# 0.1 현재 구현 현황 (As-Built)

## 구현 완료 요약

| 영역 | 구현 내용 |
|------|-----------|
| 인증 | 이메일/비밀번호 가입·로그인, 반 가입 코드, 역할(STUDENT/TEACHER/ADMIN) |
| 학생 | 홈, 사진 인증, 피드(실시간), 반별 전체 진행(`/progress`), 마이 |
| 교사 | 대시보드, 피드, 전체 진행, 격려 공지, 마이 |
| 관리자 | 전체 현황, **설정**(타이틀·기간·보상·데이터 리셋), 반 관리, 마이 |
| 인증 방식 | **스마트폰 촬영/앨범 사진 업로드 + 읽은 범위 + 한 줄 묵상** |
| 공개 범위 | 기본값 **전체 공개** (`public`), 선택 시 반 공개 |
| 피드 | 사진·묵상 표시, **좋아요**, **한 줄 댓글(최대 80자)**, Supabase Realtime LIVE |
| 진행률 | 반 참여율 / 고유 장 커버 목표 달성률, D-Day, 완주·포트럭 안내 |
| 인프라 | React+Vite+TS, Tailwind, Zustand, React Router, Supabase(Auth/DB/Storage/Realtime) |
| DB 구분 | 시그널수사와 **동일 Supabase 프로젝트** 공유 → 모든 테이블 **`wb_` 접두어** |

## 하단 네비게이션 (실제)

- **학생:** 홈 \| 인증 \| 피드 \| 전체 \| 마이
- **교사:** 대시보드 \| 피드 \| 전체 \| 공지 \| 마이
- **관리자:** 현황 \| 설정 \| 전체 \| 반관리 \| 마이

## 주요 라우트

| 경로 | 역할 | 화면 |
|------|------|------|
| `/login`, `/signup` | 게스트 | 로그인 / 회원가입(반 코드) |
| `/`, `/checkin`, `/feed`, `/class` | 학생 | 홈 / 사진 인증 / 피드 / 우리반 |
| `/progress` | 전체 역할 | 반별 진행 현황(와이드) |
| `/teacher`, `/teacher/feed`, `/teacher/announce` | 교사 | 대시보드 / 피드 / 공지 |
| `/admin`, `/admin/settings`, `/admin/projects`, `/admin/classes`, `/admin/users` | 관리자 | 현황 / 설정 / 프로젝트 / 반 / 사용자 |
| `/me` | 전체 | 마이·로그아웃 |

## Supabase 마이그레이션 파일

순서대로 SQL Editor에서 실행:

1. `(선택)` `000_drop_legacy_if_needed.sql` — 접두어 없는 초기 스키마가 있을 때만
2. `001_schema.sql` — `wb_*` 테이블·트리거
3. `002_rls.sql` — RLS
4. `seed.sql` — 복음서 4권, 샘플 반·프로젝트
5. `003_fix_signup_trigger.sql` — 가입 트리거 보강
6. `004_photo_like_realtime.sql` — 사진(`image_url`), Storage `wb-checkins`, like, Realtime
7. `005_comments_admin_reset.sql` — `wb_comments`, 관리자 활동 데이터 리셋 RPC

## 관리자 설정 (`/admin/settings`)

1. **타이틀:** 프로젝트명, 설명  
2. **기간:** 시작일, 목표일(D-Day 기준)  
3. **마지막 보상:** 포트럭 파티 일시·장소·안내문  
4. **전체 데이터 리셋:** 확인 문구 `RESET` 입력 후 해당 프로젝트의 인증·좋아요·댓글·공지 삭제 (반/사용자/프로젝트 설정은 유지)

## 사진 인증 플로우

```text
홈 → 오늘 읽었어요 → 사진 촬영/선택 → 범위·묵상 → 인증 완료 → 피드
```

- Storage 버킷: `wb-checkins` (유저별 폴더, 공개 URL)
- 사진 필수, 5MB 이하 이미지

## 피드 상호작용

- **좋아요:** `wb_encouragements.type = 'like'` 토글
- **한 줄 댓글:** `wb_comments` (내용 1~80자), 본인 삭제 가능
- **Realtime:** `wb_reading_logs`, `wb_encouragements`, `wb_comments` 변경 시 피드 갱신

---

# 1. 프로젝트 개요

## 1.1 프로젝트명


**With Bible(위드바이블)**

## 1.2 프로젝트 한 줄 정의

**고등부 학생들이 반별로 복음서를 함께 읽고 인증하며, 교사는 진행률을 확인하고 격려할 수 있는 공동체형 성경읽기 플랫폼**

## 1.3 핵심 사용자

### 학생

- 소속 반의 성경읽기 목표 확인
- **사진으로** 오늘 읽은 말씀 인증
- 읽은 범위 기록
- 한 줄 묵상 작성
- 친구들의 인증(사진·묵상) 실시간 확인
- **좋아요 / 한 줄 댓글**
- 우리 반·**전체 반** 진행률 확인

### 반 담당 교사

- 반 학생 확인
- 학생들의 인증 현황 확인
- 진행률 확인
- 격려 메시지(반 공지) 작성
- 참여가 낮은 학생 확인(교사 전용 상태)
- 전체 반 진행·피드 확인

### 고등부 관리자 / 임원 교사

- 프로젝트 생성·**설정**(타이틀·기간·보상)
- 반 생성 및 관리·가입 코드
- 학생/교사 역할·반 배정
- 전체 반 진행률·대시보드
- 공지·**활동 데이터 리셋**

---

# 2. 프로젝트 핵심 철학

## 가장 중요한 원칙

**성경읽기를 경쟁시키는 앱이 아니라 함께 읽도록 돕는 앱을 만든다.**

따라서 다음 원칙을 적용한다.

1. 학생에게 부담을 주지 않는다.
2. 읽지 못한 학생을 공개적으로 망신시키지 않는다.
3. 개인 순위보다 반 공동체의 완주를 강조한다.
4. 인증은 간단하게 한다.
5. 교사는 감시자가 아니라 격려자가 된다.
6. 앱의 목적은 사용시간 증가가 아니라 말씀읽기 실천이다.

---

# 3. MVP 범위

첫 번째 버전에서는 기능을 욕심내지 않고 아래 기능에 집중한다.

## 반드시 구현 (구현 완료)

- 로그인 / 회원가입(반 코드)
- 사용자 역할 구분
- 반 소속
- 프로젝트 생성·설정
- 성경 읽기 목표 설정
- **사진 포함** 학생 읽기 인증
- 읽은 범위 기록
- 한 줄 묵상
- 인증 피드 (**Realtime**)
- **좋아요** / **한 줄 댓글**
- 반별 진행률
- 전체 반 진행률 (`/progress`)
- 목표일 D-Day
- 교사 대시보드
- 공지사항
- 관리자 설정·데이터 리셋
- 모바일 반응형 UI

## MVP에서 제외 (유지)

- 복잡한 게임화
- 포인트 상점
- 유료 결제
- AI 묵상 분석
- 성경 전문 저작권 문제가 있는 대규모 본문 데이터 제공
- 외부 SNS 자동 공유
- 과도한 경쟁 랭킹

---

# 4. 사용자 역할

## 4.1 학생 STUDENT

권한:

- 자신의 프로필 조회
- 자신의 반 조회
- 성경읽기 **사진** 인증
- 묵상 작성
- 자신의 인증 수정/삭제
- 다른 학생 인증 조회
- **좋아요 / 한 줄 댓글**
- 반·전체 진행률 조회

## 4.2 반 교사 TEACHER

학생 권한 포함.

추가 권한:

- 담당 반 학생 조회
- 학생별 참여 현황 조회
- 반 진행률 조회
- 격려 메시지 작성
- 반 공지 작성

## 4.3 관리자 ADMIN

전체 기능:

- 프로젝트 생성/수정/**설정**(타이틀·기간·보상)
- 활동 데이터 리셋
- 반 생성/수정
- 학생 관리
- 교사 관리
- 반 배정
- 프로젝트 진행률 조회
- 전체 대시보드
- 공지 관리

---

# 5. 핵심 사용자 시나리오

## 시나리오 A. 학생

1. 로그인
2. 자신의 반 확인
3. 현재 진행 중인 복음서 프로젝트 확인
4. 오늘 읽을 범위 확인
5. 성경을 읽음
6. 「오늘 읽었어요」 버튼 클릭
7. 읽은 성경 범위 선택
8. 한 줄 묵상 입력
9. 인증 완료
10. 반 피드에 인증 게시
11. 친구의 인증에 응원
12. 반 진행률 확인

---

## 시나리오 B. 교사

1. 로그인
2. 교사 대시보드 진입
3. 담당 반 선택
4. 반 전체 진행률 확인
5. 최근 인증 확인
6. 아직 인증하지 않은 학생 확인
7. 필요한 경우 격려 메시지 작성
8. 반 목표 달성률 확인

---

## 시나리오 C. 관리자

1. 로그인
2. 관리자 대시보드
3. 새 프로젝트 생성
4. 프로젝트명 입력
5. 시작일/목표일 설정
6. 읽을 복음서 선택
7. 반 등록
8. 각 반 목표 설정
9. 학생/교사 배정
10. 프로젝트 시작

---

# 6. 화면 구조

## 학생 화면

### 6.1 홈

상단:

- 프로젝트명
- D-Day
- 우리 반 이름
- 우리 반 진행률

중앙:

**오늘의 성경읽기**

예:

> 📖 오늘 읽을 말씀
>
> 마태복음 5~6장

버튼:

**[오늘 읽었어요 ✓]**

하단:

- 최근 인증(사진·묵상)
- 좋아요 / 한 줄 댓글
- 피드·전체 진행으로 이동

---

## 6.2 인증 화면

입력 항목:

### 인증 사진 (필수)

- 카메라 촬영 (`capture=environment`)
- 앨범에서 선택
- Supabase Storage `wb-checkins` 업로드

### 읽은 범위

- 성경
- 시작 장
- 종료 장

예:

> 마태복음 5장 ~ 6장

### 오늘의 한 줄 묵상

placeholder:

> 오늘 말씀에서 가장 기억에 남은 것은?

선택 옵션:

- **전체 공개** (기본값)
- 반 친구에게 공개

버튼:

**[사진으로 인증하기]**

---

# 7. 인증 피드

인스타그램처럼 복잡한 SNS가 아니라 **사진·묵상 인증 게시판** 형태로 구현한다.  
새 인증·좋아요·댓글은 **Supabase Realtime**으로 LIVE 갱신한다.

카드 예시:

---

**(사진)**

**김민수**

📖 마태복음 5~6장

> "오늘은 예수님이 말씀하신 팔복이 가장 기억에 남았다."

❤️ 좋아요 8 · 댓글 2

**[❤️ 좋아요]**  

한 줄 댓글 입력 (최대 80자) → **[등록]**

---

피드에는 다음 정보를 표시한다.

- 이름
- 인증 날짜·공개 범위
- **인증 사진**
- 읽은 범위
- 묵상
- 좋아요 수
- 한 줄 댓글 목록

---

# 8. 반별 대시보드

## 핵심 화면

### 우리 반 복음서 챌린지

**2반**

현재 진행률:

# 68%

진행 상황:

██████████████░░░░░░

D-12

---

### 학생 참여

- 전체 학생: 15명
- 오늘 인증: 11명
- 이번 주 참여: 14명
- 완주 예상: 12명

---

### 학생별 상태

| 학생 | 최근 인증 | 누적 읽기 | 상태 |
|---|---|---:|---|
| 김민수 | 오늘 | 18장 | 🟢 |
| 이지훈 | 오늘 | 17장 | 🟢 |
| 박준영 | 2일 전 | 11장 | 🟡 |
| 최현우 | 5일 전 | 4장 | 🔴 |

상태는 공개 학생 화면에는 표시하지 않고 **교사에게만 표시한다.**

---

# 9. 진행률 계산

## 프로젝트 진행률

기본 공식:

**전체 목표 장 수 대비 반 전체 누적 인증 장 수**

단, 동일한 장을 여러 학생이 읽었다고 해서 단순히 합산하여 프로젝트 전체 진행률이 과도하게 올라가지 않도록 한다.

따라서 MVP에서는 다음 두 지표를 분리한다.

### ① 반 참여율

학생 기준:

> 인증 경험이 있는 학생 수 / 전체 학생 수

### ② 반 목표 달성률

반에서 목표로 지정된 성경 범위 중 **반 구성원이 실제로 읽은 고유 범위**를 기준으로 계산한다.

예:

목표:
마태복음 28장

학생들이 인증한 범위:

1~10장
8~15장
14~20장
18~28장

고유하게 커버된 장:
1~28장

→ 목표 달성률 100%

---

# 10. 개인 진행률

학생 개인의 진행률도 제공한다.

예:

> 김민수
>
> 마태복음
> 18 / 28장
>
> **64%**

단, 개인 순위를 강조하지 않는다.

표현:

- 나의 진행률
- 우리 반 진행률
- 오늘의 인증

등으로 구성한다.

---

# 11. 격려·좋아요·댓글

### 좋아요

인증 카드에 **❤️ 좋아요** 토글 제공.  
저장: `wb_encouragements` (`type = 'like'`), 사용자당 게시물 1개.

### 한 줄 댓글

피드에서 **최대 80자** 한 줄 댓글 작성.  
저장: `wb_comments`. 작성자 본인(또는 관리자) 삭제 가능.

### (확장) 원클릭 응원 문구

초기 명세의 응원 문구 타입(`fighting`, `together` 등)은 enum에 남아 있으며,  
현재 UI 기본 상호작용은 **좋아요 + 한 줄 댓글**이다.

---

# 12. 교사 격려 메시지

교사는 반 전체에게 메시지를 보낼 수 있다.

예:

> 📖 우리 2반!
>
> 이번 주에 벌써 마태복음 15장까지 왔어요.
> 조금 늦은 친구들도 괜찮습니다.
> 오늘부터 다시 함께 읽어봐요.
>
> 우리 반 목표일까지 함께 완주해 봅시다! 🙌

홈 화면의 반 공지 영역에 표시한다.

---

# 13. D-Day

프로젝트 목표일까지 남은 날짜를 크게 보여준다.

예:

> 🎯 완주까지
>
> **D-12**

목표일에는:

> 🎉 오늘은 완주 DAY!

로 변경한다.

---

# 14. 목표 달성 화면

반이 목표 범위를 모두 달성하면:

> 🎉 우리 반 복음서 완주!
>
> 마태복음 28장 완독
>
> 함께 읽은 친구: 15명
>
> 프로젝트 기간: 28일

버튼:

**[완주 인증 보기]**

---

# 15. 포트럭 파티 연계

웹앱의 최종 목표일과 오프라인 포트럭 파티를 연결한다.

관리자가 프로젝트 생성 시:

- 목표일
- 포트럭 파티 날짜
- 장소
- 준비물
- 공지사항

을 입력할 수 있도록 한다.

학생 홈:

> 🎉 복음서 완주 파티
>
> 8월 30일 토요일
> 오후 5시
>
> 📍 교회 3층
>
> 🍕 각자 함께 나눌 음식을 준비해주세요!

---

# 16. 전체 관리자 대시보드·설정

관리자에게 가장 중요한 화면이다.

## 전체 프로젝트 현황 (`/admin`)

### 프로젝트

우리 반 복음서 완독 프로젝트

D-12

### 전체 현황

- 전체 반: 5개
- 전체 학생: 72명
- 오늘 인증: 51명
- 전체 참여율: 83%
- 평균 진행률: 71%

---

## 반별 현황

| 반 | 학생 | 참여율 | 목표달성률 | 상태 |
|---|---:|---:|---:|---|
| 1반 | 14 | 93% | 82% | 🟢 |
| 2반 | 15 | 87% | 76% | 🟢 |
| 3반 | 13 | 62% | 51% | 🟡 |
| 4반 | 16 | 81% | 69% | 🟢 |
| 5반 | 14 | 54% | 43% | 🔴 |

관리자에게는 전체적인 현황이 한눈에 보여야 한다.

학생·교사·관리자 공통으로 **`/progress`(전체)** 화면에서도 반별 진행률 카드를 확인할 수 있다.

## 프로젝트 설정 (`/admin/settings`) — 구현됨

1. 타이틀(프로젝트명·설명)
2. 기간(시작일·목표일)
3. 마지막 보상(포트럭 일시·장소·안내)
4. 전체 활동 데이터 리셋 (`RESET` 확인, RPC `wb_admin_reset_project_activity`)

---

# 17. 데이터 모델

Supabase PostgreSQL 기준. **시그널수사와 동일 프로젝트**를 쓰므로 모든 객체에 **`wb_` 접두어**를 사용한다.

Auth `auth.users`와 1:1인 프로필만 With Bible 가입(`raw_user_meta_data.app = 'withbible'`) 시 생성한다.

## wb_profiles

- id (FK auth.users)
- name
- email
- profile_image
- role (`wb_user_role`: STUDENT | TEACHER | ADMIN)
- class_id
- created_at

## wb_classes

- id
- name
- teacher_id
- join_code
- created_at

## wb_projects

- id
- title
- description
- start_date
- end_date
- status (`draft` | `active` | `completed`)
- party_date / party_place / party_note (포트럭·보상)
- created_at

## wb_project_classes

- id
- project_id
- class_id
- target_book_id
- target_start_chapter
- target_end_chapter
- created_at

## wb_bible_books

- id
- name
- testament
- chapter_count

## wb_reading_logs

- id
- project_id
- user_id
- book_id
- start_chapter
- end_chapter
- reflection
- visibility (`class` | `public`, **기본 public**)
- **image_url**
- reading_date
- created_at
- updated_at

## wb_encouragements

- id
- reading_log_id
- user_id
- type (`fighting` | `together` | `word` | `grace` | `well_done` | **`like`**)
- created_at  
- unique (reading_log_id, user_id)

## wb_comments

- id
- reading_log_id
- user_id
- content (1~80자)
- created_at

## wb_announcements

- id
- project_id
- class_id
- author_id
- content
- created_at

## Storage

- 버킷 `wb-checkins` (public), 경로 `{userId}/{timestamp-...}.jpg`

---

# 18. 중복 인증 처리

같은 학생이 같은 날짜에 동일한 성경 범위를 여러 번 인증하지 못하도록 한다.

권장 unique 기준:

- project_id
- user_id
- book_id
- reading_date
- start_chapter
- end_chapter

다만 사용자가 잘못 입력했을 경우 수정할 수 있도록 한다.

---

# 19. 인증 신뢰성

성경읽기는 온라인에서 실제로 읽었는지 완벽하게 검증하기 어렵다.

따라서 MVP에서는 **강제 검증 시스템을 만들지 않는다.**

핵심은:

> 인증을 감시하기 위한 시스템이 아니라 말씀읽기를 독려하기 위한 시스템

이라는 점을 유지한다.

---

# 20. 개인정보 및 공개 범위

학생 서비스이므로 개인정보 최소화를 원칙으로 한다.

### 학생에게 공개

- 이름
- 프로필 이미지
- 반
- 읽은 범위
- 묵상
- 응원 수

### 학생에게 비공개

- 다른 학생의 미참여 여부
- 교사가 보는 개인별 경고 상태
- 관리자용 상세 통계

### 교사에게 공개

담당 반 학생의 진행 현황.

### 관리자에게 공개

전체 학생 및 반 현황.

---

# 21. 인증 및 로그인

MVP에서는 Supabase Auth 사용.

### 1차 (구현됨)

이메일/비밀번호 로그인·회원가입

가입 메타데이터:

- `app: 'withbible'` — With Bible 전용 `wb_profiles` 생성 트리거 조건
- `name`, `join_code`, `role`

### 2차

Google 로그인

교회 현장에서 학생 계정을 쉽게 관리해야 하므로 이후에는 **관리자가 학생 계정을 초대하거나 가입코드를 통해 반을 배정하는 방식**도 검토한다. (가입 코드 방식은 구현됨)

---

# 22. 반 가입 방식

학생이 회원가입 후 관리자에게 요청하는 방식은 운영 부담이 크다.

추천 방식:

### 반 코드 방식

교사가 반 코드를 생성한다.

예:

> 2반 가입 코드: `BIBLE26-2`

학생은 가입 시 코드를 입력한다.

그러면 자동으로 해당 반에 배정된다.

관리자는 이후 학생 명단을 확인한다.

---

# 23. UI/UX 방향

## 디자인 키워드

- 따뜻함
- 신앙적
- 청소년 친화적
- 부담 없음
- 모바일 우선
- 깔끔함

성경 앱처럼 너무 무겁거나 고전적인 디자인보다는 **청소년 커뮤니티 앱에 가까운 디자인**을 사용한다.

---

# 24. 네비게이션 (구현)

학생:

**홈 | 인증 | 피드 | 전체 | 마이**

교사:

**대시보드 | 피드 | 전체 | 공지 | 마이**

관리자:

**현황 | 설정 | 전체 | 반관리 | 마이**

(`프로젝트`·`사용자` 화면은 `/admin/projects`, `/admin/users`로 유지하며 설정·반관리에서 연계 운영한다.)

---

# 25. 모바일 우선

실제 사용 환경은 대부분 스마트폰이다.

따라서:

- 모바일 1순위
- PC 2순위
- 반응형 웹
- PWA 지원 고려

인증 과정은 **3번 이상의 화면 이동을 하지 않도록** 한다.

최종적으로:

**홈 → 오늘 읽었어요 → 사진·범위·묵상 → 인증 완료 → 피드**

가 가장 이상적이다.

---

# 26. 기술 스택 (구현)

### Frontend

- React 19 + Vite 8
- TypeScript
- Tailwind CSS 4
- UI  primitive (Button/Input/Card 등, shadcn 스타일)

### Backend

- Supabase (시그널수사와 **동일 프로젝트**)
- PostgreSQL (`wb_*` 테이블)
- Supabase Auth
- Supabase Storage (`wb-checkins`)
- Supabase Realtime

### State / Routing

- Zustand
- React Router

### Deployment

- Vercel 전제

### Version Control

- GitHub

---

# 27. 아키텍처 원칙

컴포넌트에서 Supabase를 직접 호출하지 않는다.

권장 구조:

```text
src/
├── components/
├── pages/
│   ├── student/
│   ├── teacher/
│   └── admin/
├── layouts/
├── services/
│   ├── authService.ts
│   ├── projectService.ts
│   ├── readingService.ts
│   ├── classService.ts
│   ├── announcementService.ts
│   ├── commentService.ts
│   ├── progressService.ts
│   └── storageService.ts
├── stores/
│   ├── authStore.ts
│   └── projectStore.ts
├── types/
├── utils/
└── lib/
    ├── supabase.ts
    └── tables.ts   # wb_* 테이블명 상수
supabase/
├── migrations/
└── seed.sql
```

---

# 28. 개발 단계

## Phase 1 — 프로젝트 기반

- React + Vite + TypeScript
- Tailwind
- shadcn/ui
- Supabase 연결
- 기본 라우팅
- 환경변수 설정

## Phase 2 — 인증

- 로그인
- 회원가입
- 역할
- 반 배정
- 로그아웃

## Phase 3 — 프로젝트

- 프로젝트 생성
- 프로젝트 조회
- 복음서 목표 설정
- 목표일

## Phase 4 — 학생 기능

- 오늘의 읽기
- 읽기 인증
- 묵상
- 인증 피드
- 응원

## Phase 5 — 반 기능

- 반 진행률
- 학생 참여 현황
- 반 공지
- 교사 격려

## Phase 6 — 관리자

- 전체 대시보드
- 반 관리
- 학생 관리
- 프로젝트 관리

## Phase 7 — 완주

- 완주율
- 완주 화면
- 목표일
- 포트럭 파티 정보

## Phase 8 — QA

- 모바일 테스트
- 권한 테스트
- 데이터 정합성
- 중복 인증 테스트
- RLS 정책 테스트

---

# 29. RLS 보안

Supabase Row Level Security를 반드시 적용한다.

### 학생

자신의 reading_logs 생성/수정/삭제 가능.

다른 학생의 reading_logs는 읽기만 가능.

### 교사

자신이 담당하는 class의 데이터 접근 가능.

### 관리자

전체 데이터 접근 가능.

학생이 URL을 조작해서 다른 반의 민감한 데이터를 볼 수 없어야 한다.

---

# 30. MVP 성공 기준

다음 시나리오가 실제로 동작하면 MVP 완료로 판단한다.

### 관리자

1. 프로젝트 생성
2. 반 생성
3. 학생 등록
4. 교사 배정
5. 목표 복음서 설정
6. 목표일 설정

### 학생

1. 로그인
2. 자신의 반 확인
3. 오늘 읽을 범위 확인
4. **사진 촬영/선택 후** 읽기 인증
5. 묵상 입력
6. 인증 피드 확인 (**Realtime**)
7. **좋아요·한 줄 댓글**
8. 반·전체 진행률 확인

### 교사

1. 로그인
2. 담당 반 확인
3. 반 진행률 확인
4. 학생별 참여 확인
5. 격려 메시지 작성

### 관리자

1. `/admin/settings`에서 타이틀·기간·보상 설정
2. 필요 시 활동 데이터 리셋
3. 반·사용자 배정

### 완주

1. 목표 달성
2. 완주 화면 표시
3. 목표일 도달
4. 포트럭 파티 정보 확인

---

# 31. 향후 확장 기능

MVP가 성공한 이후 추가한다.

## 게임화

- 연속 읽기 streak
- 배지
- 반 공동 XP
- 완주 배지

단, 개인 경쟁보다 공동체 중심으로 설계한다.

## AI 기능

향후 AI를 붙일 수 있다.

예:

### AI 말씀 질문

인증 후:

> 오늘 읽은 말씀에서 가장 마음에 남은 부분은 무엇인가요?

### AI 묵상 도우미

학생이 작성한 묵상에 대해:

- 질문
- 관련 성경구절
- 생각해볼 포인트

등을 제공한다.

단, AI가 신앙적 판단이나 설교를 대신하지 않도록 한다.

---

# 32. 중요한 차별점

일반적인 성경 앱과 다르게 이 서비스의 핵심은 **성경 콘텐츠 제공 자체가 아니다.**

핵심은:

**Community Reading**

즉,

> **'우리 반이 함께 읽는다.'**

이다.

따라서 홈 화면에서도 개인의 성경 읽기보다

**우리 반 진행률**

을 중요한 정보로 보여준다.

---

# 33. 서비스 핵심 루프

전체 서비스는 아래 루프를 반복해야 한다.

```text
읽는다
 ↓
인증한다
 ↓
친구가 본다
 ↓
응원한다
 ↓
반 진행률이 올라간다
 ↓
교사가 격려한다
 ↓
다시 읽는다
```

이 루프가 서비스의 핵심이다.

---

# 34. Cursor 개발 원칙

Cursor는 다음 원칙을 지킨다.

1. 먼저 프로젝트 구조를 분석한다.
2. 한 번에 모든 기능을 구현하지 않는다.
3. Phase 단위로 구현한다.
4. 각 Phase 완료 후 실행 및 오류 확인을 한다.
5. TypeScript 타입 오류를 해결한다.
6. Supabase RLS를 반드시 적용한다.
7. 모바일 화면을 우선 구현한다.
8. 더미 데이터로 먼저 UI를 검증한다.
9. 실제 Supabase 연결 후 다시 테스트한다.
10. 기존 기능을 깨뜨리지 않는다.
11. 기능 구현 후 변경 내용을 요약한다.
12. 임의로 라이브러리를 추가하지 않는다.

---

# 35. Cursor 최초 실행 프롬프트

아래 프롬프트를 Cursor Agent에게 전달한다.

---

## ROLE

너는 시니어 React/TypeScript/Supabase 개발자이자 UX 엔지니어다.

고등부 교회에서 실제로 사용할 수 있는 **복음서 공동 성경읽기 웹앱**을 개발한다.

이 프로젝트의 목적은 성경읽기를 경쟁시키는 것이 아니라 학생들이 반 단위로 함께 말씀을 읽고 서로 격려하도록 돕는 것이다.

## 먼저 할 일

바로 코딩하지 말고 현재 프로젝트를 분석한다.

1. package.json 확인
2. 기존 디렉토리 구조 확인
3. 기존 UI 확인
4. 설치된 라이브러리 확인
5. Supabase 연결 여부 확인
6. 환경변수 확인

그 다음 이 개발명세서의 요구사항을 기준으로 구현 계획을 작성한다.

## 개발 순서

반드시 다음 순서로 진행한다.

### Phase 1

프로젝트 기반 및 UI shell

### Phase 2

인증 및 사용자 역할

### Phase 3

프로젝트/반 관리

### Phase 4

성경읽기 인증

### Phase 5

피드/응원

### Phase 6

반별 대시보드

### Phase 7

관리자 대시보드

### Phase 8

완주 및 포트럭 파티

### Phase 9

QA 및 모바일 최적화

각 Phase가 끝나면:

- 구현 내용
- 변경 파일
- 테스트 결과
- 남은 작업
- 발생한 오류

를 정리한다.

## 절대 지켜야 할 UX 원칙

학생이 성경을 읽고 인증하는 과정은 최대한 짧게 만든다.

**홈 → 오늘 읽었어요 → 범위 선택 → 인증**

4단계 이내를 목표로 한다.

## 중요한 제품 원칙

이 앱은 성경읽기 검사 앱이 아니다.

학생이 읽지 않았다는 사실을 공개적으로 표시하지 않는다.

학생에게는:

- 나의 진행률
- 우리 반 진행률
- 친구들의 인증
- 응원

을 중심으로 보여준다.

교사에게만 학생별 참여 현황을 제공한다.

## 기술 원칙

React + Vite + TypeScript

Tailwind CSS + shadcn/ui

Supabase Auth + PostgreSQL

Zustand

React Router

Vercel 배포를 전제로 한다.

컴포넌트에서 Supabase를 직접 호출하지 말고 service layer를 사용한다.

Supabase RLS를 반드시 적용한다.

모바일 우선으로 개발한다.

---

# 36. 최종 제품 방향

이 앱의 성공 여부는 기능의 많고 적음이 아니라 다음 질문으로 판단한다.

> **학생이 오늘 성경을 읽고 싶어지는가?**

그리고 두 번째 질문:

> **친구들이 함께 읽고 있다는 느낌을 받을 수 있는가?**

마지막 질문:

> **교사가 학생을 관리하는 것이 아니라 자연스럽게 격려할 수 있는가?**

이 세 가지가 충족되는 방향으로 개발한다.
